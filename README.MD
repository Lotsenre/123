# 🚂 ВагоноМесто - На что обратить внимание

## ✅ Новые функции

### Обязательная авторизация
- При загружении сайта сразу отображается экран входа/регистрации
- Основное покупки билетов доступны только после входа
- JWT токены сохраняются в localStorage браузера

### Современный дизайн
- Градиентные фоны и модернные компоненты
- Навигация через табы
- Читаемые поля для информации во все этапы

## 🚀 Быстрый запуск (клонировать и запустить!)

### 1. Клонировать репозиторий
```bash
git clone https://github.com/Lotsenre/123.git
cd 123
```

### 2. Создать виртуальное окружение
```bash
python -m venv venv
```

### 3. Активировать виртуальное окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 4. Установить зависимости
```bash
pip install -r requirements.txt
```

### 5. Запустить приложение
```bash
python main.py
```

### 6. Открыть в браузере
```
http://localhost:8000/
```

**Готово!** База данных и настройки уже включены в репозиторий - ничего дополнительно настраивать не нужно.

## 🔐 Панель администратора

- URL: `http://localhost:8000/admin`
- Логин: `admin`
- Пароль: `admin123`

## 👁 Нюансы

### Регистрация
- Введите имя, email и пароль (6+ символов)
- Роль будет автоматически сеть на "user" (id=1)

### Вход
- Введите email и пароль данные
- Получите JWT токен

### API endpoints
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход и получение JWT
- `GET /api/auth/me` - Проверка токена и получение данных пользователя (Bearer token required)
- `POST /api/auth/logout` - Очистка cookie

### Остальные `/api/tickets*` routes
- Оченидают Authorization header: `Bearer <JWT token>`
- Все ныне защищены авторизацией
